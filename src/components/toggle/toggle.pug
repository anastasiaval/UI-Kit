//-
    options.checked - bool - if true checked='checked'
    options.text - string - option text

mixin toggle(options)
    label.toggle
        input.toggle__input(type='checkbox' checked= options.checked)
        span.toggle__box
        |   !{options.text}

